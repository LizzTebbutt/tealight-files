from tealight.robot import (move, 
                            turn, 
                            look, 
                            touch, 
                            smell, 
                            left_side, 
                            right_side)

if left_side() != "wall":
  turn() 1
  move
else if right_side() != "wall": 
  turn() -1
  move
  
  
